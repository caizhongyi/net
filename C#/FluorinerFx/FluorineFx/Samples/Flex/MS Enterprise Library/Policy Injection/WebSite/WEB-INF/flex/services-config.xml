<?xml version="1.0" encoding="utf-8" ?> 
<services-config>
	<factories>
		<factory id="piab" class="ServiceLibrary.PIABFactory"/>
	</factories>
	
    <services>
        <service id="remoting-service" class="flex.messaging.services.RemotingService" messageTypes="flex.messaging.messages.RemotingMessage">
			    <destination id="fluorine-piab">
            <channels>
              <channel ref="my-amf"/>
            </channels>			
				    <properties>
					    <factory>piab</factory>
					    <source>ServiceLibrary.Sample, ServiceLibrary</source>
				    </properties>
			    </destination>
        </service>
    </services>
    
    <channels>
        <channel-definition id="my-amf" class="mx.messaging.channels.AMFChannel">
            <endpoint uri="http://{server.name}:{server.port}/{context.root}/Gateway.aspx" class="flex.messaging.endpoints.AMFEndpoint"/>
        </channel-definition>
    </channels>
</services-config>