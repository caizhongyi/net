using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace czy.Wpf.Library
{
    public  class Util
    {
        ///// <summary>    
        ///// 实现数据的四舍五入法　　
        ///// /// </summary> 
        ///// /// <param name="v">要进行处理的数据</param>    
        ///// <param name="x">保留的小数位数</param>  
        ///// /// <returns>四舍五入后的结果</returns> 
        //public   static double Round(double v, int x)
        //{
        //    bool isNegative = false;
        //    //如果是负数       
        //    if (v < 0)
        //    { isNegative = true; v = -v; }
        //    int IValue = 1; 
        //    for (int i = 1; i <= x; i++) 
        //    { IValue = IValue * 10; } 
        //    double Int = Math.Round(v * IValue + 0.5, 0); v = Int / IValue; 
        //    if (isNegative) { v = -v; } 
        //    return v;
        //}
    }
}
